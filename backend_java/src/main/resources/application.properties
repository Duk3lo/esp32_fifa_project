server.port=8081

# Base de datos: Usa variables de entorno. Si no existen, usa tus credenciales locales y crea la BD.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/pronosticosMundial?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:dukelo}
spring.datasource.password=${DB_PASSWORD:Jhosue2004!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# El ESP32 se anuncia por mDNS como "esp32.local".
# Si tu red local o sistema no resuelve .local, puedes sobreescribir con la IP usando la variable de entorno.
esp32.url=${ESP32_URL:http://esp32.local}
esp32.ws=${ESP32_WS:ws://esp32.local/ws}